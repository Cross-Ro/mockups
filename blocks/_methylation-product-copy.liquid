{%- liquid
  assign value_card_count = 0
  for child in block.blocks
    if child.id contains '_vc_' or child.id contains '__vc_'
      assign value_card_count = value_card_count | plus: 1
    endif
  endfor
-%}

<div class="methylation-hero__copy" {{ block.shopify_attributes }}>

  <div class="methylation-hero__header-group">
    {%- for child in block.blocks -%}
      {%- assign cid = child.id -%}
      {%- if cid contains '_eyebrow' or cid contains '_headline_serif' or cid contains '_headline_script' or cid contains '_tagline' -%}
        {% render child, section: section %}
      {%- endif -%}
    {%- endfor -%}
  </div>

  <div class="methylation-hero__description-group">
    {%- for child in block.blocks -%}
      {%- if child.id contains '_body' -%}
        {% render child, section: section %}
      {%- endif -%}
    {%- endfor -%}
  </div>

  {%- if value_card_count > 0 -%}
    <div class="methylation-hero__value-box">
      <div class="methylation-hero__value-cards" data-card-count="{{ value_card_count }}">
        {%- for child in block.blocks -%}
          {%- if child.id contains '_vc_' -%}
            {% render child, section: section %}
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  {%- endif -%}

  <div class="methylation-hero__purchase-group">
    {%- for child in block.blocks -%}
      {%- if child.id contains '_price' or child.id contains '_cta' -%}
        {% render child, section: section %}
      {%- endif -%}
    {%- endfor -%}
  </div>

</div>

{% schema %}
{
  "name": "Product copy",
  "settings": [],
  "blocks": [
    { "type": "_methylation-eyebrow" },
    { "type": "_methylation-headline-serif" },
    { "type": "_methylation-headline-script" },
    { "type": "_methylation-tagline" },
    { "type": "_methylation-body" },
    { "type": "_methylation-value-card" },
    { "type": "_methylation-price" },
    { "type": "_methylation-cta" }
  ],
  "presets": [
    {
      "name": "Product copy"
    }
  ]
}
{% endschema %}
